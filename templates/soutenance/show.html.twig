{% extends 'base.html.twig' %}

{% block title %}Soutenance{% endblock %}

{% block body %}
    <h1>Soutenance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ soutenance.id }}</td>
            </tr>
            <tr>
                <th>Numjury</th>
                <td>{{ soutenance.numjury }}</td>
            </tr>
            <tr>
                <th>Date_soutenance</th>
                <td>{{ soutenance.dateSoutenance ? soutenance.dateSoutenance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ soutenance.note }}</td>
            </tr>
            
        </tbody>
    </table>

    <a class="btn btn-info" style="margin-left:340px;transform:translate(100px,19px);"   href="{{ path('app_soutenance_index') }}">back to list</a>

    <a  class="btn btn-success" style="transform:translate(250px,18px)" href="{{ path('app_soutenance_edit', {'id': soutenance.id}) }}">edit</a>

    {{ include('soutenance/_delete_form.html.twig') }}
{% endblock %}
