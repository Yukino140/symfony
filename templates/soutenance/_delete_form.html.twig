<form method="post" action="{{ path('app_soutenance_delete', {'id': soutenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ soutenance.id) }}">
    <button style="margin-left:580px;transform: translate(0px,-20px);" class="btn btn-danger">Delete</button>
</form>
